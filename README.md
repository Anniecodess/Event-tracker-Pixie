📅 Event Tracker System (Python)
📌 Overview

The Event Tracker System is a Python-based backend tool designed to discover, track, and store upcoming events for a selected city.
It fetches real-time event data using a public events API, processes the data, and stores it in an Excel file that opens automatically after execution.

This project focuses on data extraction, city-based filtering, and structured storage, following a clean and modular architectur


### Features
- Modular scrapers
- Excel-based storage
- Config-driven architecture

### Setup
```bash
python -m venv venv
pip install -r requirements.txt
python main.py
